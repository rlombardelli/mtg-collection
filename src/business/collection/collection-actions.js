import {createAction} from 'redux-act';

export const addCardToCollection = createAction('collection.addCardToCollection');
export const removeCardFromCollection = createAction('collection.removeCardFromCollection');
